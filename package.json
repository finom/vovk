{
  "name": "vovk-monorepo",
  "version": "1.0.0-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "packageManager": "npm@11.7.0",
  "description": "RESTful RPC for Next.js - Transforms Next.js into a powerful REST API platform with RPC capabilities.",
  "scripts": {
    "ncu": "turbo run ncu --dangerously-disable-package-manager-check && npm run --prefix test ncu && npm-check-updates -u && npm i --legacy-peer-deps",
    "test:main": "npm --prefix test run generate && pm --prefix test run build-other-compiled-sources && npm run lint-nofix && turbo run tsc --dangerously-disable-package-manager-check && tsc --project ./test && npm run unit",
    "test:cli": "npm --prefix packages/vovk-cli run test",
    "test:py": "npm --prefix packages/vovk-python run test",
    "test:rs": "npm --prefix packages/vovk-rust run test",
    "test": "npm run build && npm run test:main && npm run test:rs && npm run test:py && npm run test:cli",
    "test-only": "npm run build && npm --prefix test run generate && cross-env PORT=3210 concurrently \"sleep 30 && NODE_ENV=test node --experimental-strip-types --test --test-only --test-concurrency=1 ./test/**/*.test.ts\" \"npm run serve:unit\" --kill-others --success first",
    "unit": "cross-env PORT=3210 concurrently \"npm run serve:unit\" \"sleep 60 && npm run test:unit\" --kill-others --success first",
    "test:unit": "NODE_ENV=test node --experimental-strip-types --test --test-concurrency=1 ./test/**/*.test.ts",
    "prettier": "prettier --write .",
    "serve:unit": "npm --prefix test run generate && npm --prefix test run dev",
    "lint-nofix": "eslint .",
    "lint": "npm run lint-nofix -- --fix",
    "build": "npm run rm-dist --prefix packages/vovk && npm run build --prefix packages/vovk && npm run build --prefix packages/vovk-cli && turbo run build --force --dangerously-disable-package-manager-check",
    "vpub-check-vars": "if [ -z \"$VERSION_TYPE\" ] || [ -z \"$PACKAGE\" ] || [ -z \"$NPM_TAG\" ]; then echo 'Error: some vars are not set'; exit 1; fi;",
    "vpub:version": "npm version --prefix ./packages/$PACKAGE $VERSION_TYPE --no-git-tag-version",
    "vpub:bump-deps": "node --experimental-strip-types ./scripts/bump-dependencies.ts",
    "vpub:build": "npm run build",
    "vpub-commit": "if [ \"$NPM_TAG\" != \"draft\" ] && [ \"$NPM_TAG\" != \"beta\" ] && [ \"$NPM_TAG\" != \"latest\" ]; then echo \"TODO: Remove NPM_TAG latest check before release\"; git commit -am \"$VERSION_TAG\"; fi",
    "vpub:commit-and-publish": "VERSION_TAG=$PACKAGE-v$(node -p \"require('./packages/$PACKAGE/package.json').version\") npm run vpub-commit && npm run --prefix ./packages/$PACKAGE npm-publish -- --tag=$NPM_TAG && git push && git push --tags",
    "vpub": "npm run vpub-check-vars && npm t && npm-run-all -s vpub:*",
    "vpub-notest": "npm run vpub-check-vars && npm-run-all -s vpub:*",
    "patch": "NPM_TAG=latest VERSION_TYPE=patch npm run vpub",
    "minor": "NPM_TAG=latest VERSION_TYPE=minor npm run vpub",
    "BREAKING-major": "NPM_TAG=latest VERSION_TYPE=major npm run vpub",
    "beta": "NPM_TAG=latest VERSION_TYPE=\"prerelease --preid=beta\" npm run vpub",
    "beta-all": "npm t && for PACKAGE in vovk vovk-cli vovk-client vovk-ajv vovk-python vovk-rust; do PACKAGE=$PACKAGE npm run beta-notest-IAMSURE; done",
    "beta-notest-IAMSURE": "NPM_TAG=latest VERSION_TYPE=\"prerelease --preid=beta\" npm run vpub-notest",
    "BREAKING-beta-major": "NPM_TAG=beta VERSION_TYPE=\"premajor --preid=beta\" npm run vpub",
    "draft": "NPM_TAG=draft VERSION_TYPE=\"prerelease --preid=draft\" npm run vpub",
    "draft-notest": "NPM_TAG=draft VERSION_TYPE=\"prerelease --preid=draft\" npm run vpub-notest",
    "draft-notest-all": "for PACKAGE in vovk vovk-cli vovk-client vovk-ajv vovk-python vovk-rust; do PACKAGE=$PACKAGE npm run draft-notest; done"
  },
  "engines": {
    "node": ">=23.0.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": true,
    "singleQuote": true,
    "printWidth": 120
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/finom/vovk.git"
  },
  "keywords": [
    "nextjs",
    "router"
  ],
  "author": "Andrey Gubanov",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/finom/vovk/issues"
  },
  "homepage": "https://github.com/finom/vovk#readme",
  "devDependencies": {
    "@eslint/compat": "^2.0.0",
    "@types/node": "^25.0.3",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "npm-check-updates": "^19.2.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.7.4",
    "turbo": "^2.7.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.51.0",
    "next": "^16.1.1"
  }
}
