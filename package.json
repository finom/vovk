{
  "name": "vovk-monorepo",
  "version": "0.0.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "RESTful RPC for Next.js - Transforms Next.js into a powerful REST API platform with RPC capabilities.",
  "packageManager": "npm@10.8.3",
  "scripts": {
    "ncu": "turbo run ncu && npm-check-updates -u -x @typescript-eslint/eslint-plugin,@typescript-eslint/parser,eslint && npm i",
    "test": "npm --prefix test run generate && npm run lint-nofix && tsc --noemit && tsc --project ./packages/vovk-cli/tsconfig.json --noemit && npm run unit",
    "unit": "PORT=3210 concurrently \"sleep 30 && npm run test:unit\" \"npm run serve:unit\" --kill-others --success first",
    "unit:watch": "PORT=3210 concurrently \"sleep 30 && npm run test:unit:watch\" \"npm run serve:unit\" --kill-others --success first",
    "test:unit": "jest",
    "test:unit:watch": "jest --watch",
    "serve:unit": "npm --prefix test run generate && npm --prefix test run build && npm --prefix test run start",
    "lint-nofix": "eslint . --ext .ts,.tsx",
    "lint": "npm run lint-nofix -- --fix",
    "build": "turbo run build --force",
    "vpub-check-vars": "if [ -z \"$VERSION_TYPE\" ] || [ -z \"$PACKAGE\" ] || [ -z \"$NPM_TAG\" ]; then echo 'Error: some vars are not set'; exit 1; fi;",
    "vpub:version": "npm version --prefix ./packages/$PACKAGE $VERSION_TYPE --no-git-tag-version",
    "vpub:bump-deps": "ts-node ./scripts/bump-dependencies.ts",
    "vpub:build": "npm run build",
    "vpub:commit-and-publish": "TAG=$PACKAGE-v$(node -p \"require('./packages/$PACKAGE/package.json').version\") && git commit -am \"$TAG\" && npm run --prefix ./packages/$PACKAGE npm-publish --tag=$NPM_TAG && git push && git push --tags",
    "vpub": "npm run vpub-check-vars && npm t && npm-run-all -s vpub:*",
    "vpub-notest": "npm run vpub-check-vars && npm-run-all -s vpub:*",
    "beta": "NPM_TAG=beta VERSION_TYPE=\"prerelease --preid=beta\" npm run vpub",
    "beta-notest": "NPM_TAG=beta VERSION_TYPE=\"prerelease --preid=beta\" npm run vpub-notest",
    "beta-patch": "NPM_TAG=beta VERSION_TYPE=\"prepatch --preid=beta\" npm run vpub",
    "beta-minor": "NPM_TAG=beta VERSION_TYPE=\"preminor --preid=beta\" npm run vpub",
    "BREAKING-beta-major": "NPM_TAG=beta VERSION_TYPE=\"premajor --preid=beta\" npm run vpub",
    "patch": "NPM_TAG=latest VERSION_TYPE=patch npm run vpub",
    "minor": "NPM_TAG=latest VERSION_TYPE=minor npm run vpub",
    "BREAKING-major": "NPM_TAG=latest VERSION_TYPE=major npm run vpub"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": true,
    "singleQuote": true,
    "printWidth": 120
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/finom/vovk.git"
  },
  "keywords": [
    "nextjs",
    "router"
  ],
  "author": "Andrii Gubanov",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/finom/vovk/issues"
  },
  "homepage": "https://github.com/finom/vovk#readme",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/lodash": "^4.17.7",
    "@types/node": "^22.5.1",
    "@types/supertest": "^6.0.2",
    "@typescript-eslint/eslint-plugin": "^7.16.1",
    "@typescript-eslint/parser": "^7.16.1",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "glob": "^11.0.0",
    "jest": "^29.7.0",
    "lodash": "^4.17.21",
    "npm-check-updates": "^17.1.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.3.3",
    "puppeteer": "^23.2.1",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "turbo": "^2.1.1",
    "typescript": "^5.5.4",
    "yup": "^1.4.0",
    "zod": "^3.23.8"
  }
}
