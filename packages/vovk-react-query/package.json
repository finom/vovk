{
  "name": "vovk-react-query",
  "version": "0.0.1-draft.45",
  "description": "useQuery and useMutation hooks for Vovk.ts",
  "scripts": {
    "build": "rm -f *.tsbuildinfo && npm run build:cjs && npm run build:esm",
    "build:esm": "tsc -P tsconfig.build.esm.json --module esnext",
    "build:cjs": "cp index.mts index.cts && tsc -P tsconfig.build.cjs.json",
    "tsc": "tsc --noEmit",
    "npm-publish": "npm publish",
    "ncu": "npm-check-updates -u",
    "build-test": "tsc -P tsconfig.test.json",
    "pre-test": "npm run build && npm run build-test",
    "test-only": "npm run pre-test && node --test --test-only test/test/**/*.mjs",
    "unit": "npm run pre-test && node --test --test-concurrency=1 test/*.mjs",
    "test": "concurrently 'sleep 20 && npm run unit' 'npm run generate --prefix ../../test && npm run build --prefix ../../test && npm run start --prefix ../../test' --kill-others --success first"
  },
  "main": "./index.cjs",
  "module": "./index.mjs",
  "types": "./index.d.mts",
  "exports": {
    ".": {
      "import": "./index.mjs",
      "require": "./index.cjs"
    }
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/finom/vovk.git"
  },
  "keywords": [
    "reqct-query"
  ],
  "author": "Andrii Gubanov",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/finom/vovk/issues"
  },
  "homepage": "https://github.com/finom/vovk#readme",
  "peerDependencies": {
    "@tanstack/react-query": "^5.66.0",
    "react": "^19.0.0",
    "vovk": "^3.0.0-draft.85"
  },
  "devDependencies": {
    "@testing-library/react": "^16.2.0",
    "@types/jsdom": "^21.1.7",
    "@types/react": "^19.0.8",
    "jsdom": "^26.0.0",
    "react": "^19.0.0"
  }
}